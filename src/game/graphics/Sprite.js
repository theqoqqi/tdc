
export default class Sprite {

    constructor({atlas, x, y, width, height}) {
        this.atlas = atlas;
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}